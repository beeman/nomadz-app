import { AppView } from '@/components/app-view'
import { AppText } from '@/components/app-text'
import * as React from 'react'

export function TripsFeatureDetail() {
  return (
    <AppView>
      <AppText variant="titleMedium">Trip Detail page</AppText>
      <AppText>Trips Detail.</AppText>
    </AppView>
  )
}
